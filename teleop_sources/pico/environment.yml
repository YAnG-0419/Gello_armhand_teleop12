name: franka-teleop-pico
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pybind11
  - pyyaml
  # Hand retargeting: scipy provides L-BFGS-B, pybullet provides the URDF
  # forward kinematics and analytic Jacobians the objective needs.
  - scipy
  - pip:
      - pybullet==3.2.7
