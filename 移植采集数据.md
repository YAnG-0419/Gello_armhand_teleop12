# Harvest 数据采集移植到 gello-retarget

把 Harvest 的「ROS bag → 手动转 LeRobot v2」能力接到本仓库的 **GELLO + MANUS→Wuji** 遥操上，用来采训练数据。

本文件是实施清单，不是代码。开始改代码前先读本仓库 `AGENTS.md`、`CLAUDE.md`、`README.md`、`docs/HARDWARE_DEPLOY.md`，并保留已有本地修改和硬件配置。

## 1. 目标

以 `/home/user/lpy/gello-retarget` 为唯一主工程：

- 保留现有 GELLO→双 FR3、安全网关、Operator GUI、默认 O30i 控制链，以及可选的 Wuji / Pico / MoveIt。
- 第一阶段只支持 **GELLO 臂 + Wuji 手 + 三相机** 的 Harvest 格式采集。Pico 臂可作为后续模式，不作为第一期阻塞项。
- 在线录制只写 ROS bag；转换必须由操作者在录制结束后手动执行。
- 数据采集是只读子系统：任何采集故障都不能阻塞或接管遥操控制。

## 2. 只读参考（禁止当运行时依赖）

Sibling 仓库只读。需要的代码先拷进本仓库再改。

| 路径 | 用途 |
| --- | --- |
| `/home/user/llx/harvest_ws/teleop` | 原始采集设计、三相机、recorder/converter、OpenSpec |
| `/home/user/llx/harvest_ws/teleop/ros2/src/data_collector` | SPACE/D/Ctrl-C、episode 状态、LeRobot v2 转换 |
| `/home/user/llx/harvest_ws/teleop/ros2/src/orbbec_bringup` | 按序列号绑定 cam0/cam1/cam2 |
| `/home/user/llx/harvest_ws/teleop/ros2/src/vendor/orbbec_camera*` | LICENSE/NOTICE、SDK 2.8.6、EULA 门禁 |
| `/home/user/llx/harvest_ws/teleop/openspec/changes/support-pico-gello-data-collection` | GELLO/Pico 分模式录制、同一 LeRobot 契约 |
| `/home/user/llx/harvest_ws/wuji-openpi` | 训练侧格式差异（文档保留，本任务不改 OpenPI） |
| `/home/user/llx/Pico_armhand_teleop-main` | Codex 已把 Harvest collector 接到本系安全网关和 Wuji UDP 遥测；优先复用，不要从 Harvest 整目录重搬 |

Pico 仓库工作区未提交、未写完成报告，且缺 fake Franka 短 episode / 真机验收。拷过来之后仍要按本清单在本仓库重新测试。

不要从 Harvest 搬入：`pico_host`、`pico_teleop`、`manus_wuji_teleop`、`wuji_hand_driver`、Franka controller、第二套 Operator。

## 3. 本仓库现状（不要覆盖）

| 已有能力 | 处理方式 |
| --- | --- |
| `ros_ws/src/teleop_data` | O30i/G20 + 单路 `/camera/*` 的另一套 schema。**保留且独立**，不要改成 Harvest 格式，也不要让 Harvest recorder 去订 `/cb_*`。 |
| Docker `orbbec` 服务 | 单路网络 Gemini，topic 为 `/camera/color|depth/...`。Harvest 三相机 bringup 必须分开，录制时禁止和该服务或 `OrbbecViewer` 抢同一台相机。 |
| `apps/left_wuji_dataset_recorder` | 进程内左臂 NPZ，不是 Harvest bag/LeRobot。不要扩展成正式采集链。 |
| `start_wuji_teleop.sh` | 第一期遥操入口。Operator 持有唯一 Wuji 连接。 |
| `start_teleop.sh` | 默认 O30i，与本次 Harvest/Wuji 采集无关。 |
| `teleop_core.contract` | 已有臂 topic/关节名。**还没有** Wuji 20 关节名和手 telemetry topic。 |

## 4. 不可破坏的边界

1. 不创建第二个 Pico、MANUS 或 Wuji SDK client。
2. 只有本仓库 safety gateway 可以发布 FR3 command bus。
3. Recorder、相机和 telemetry receiver 不得向机械臂或手发布控制命令。
4. 不替换现有 `teleop_core` 安全网关、`pico_teleop_bridge`、`teleop_interfaces` 控制消息、Franka controller、Operator、`teleop_data`。
5. 不把 Harvest 的 Pico/GELLO 控制栈或 Wuji ROS driver 搬进来。
6. 不自动转换、上传或删除源 ROS bag。
7. 不在未获用户明确授权时启动真实硬件运动。
8. 不削弱 freshness、acquisition、limit、slew 检查来让测试通过。
9. 不添加 glue-dispenser 术语或功能。

## 5. 关键差异（必须适配，不能原样复制）

### 5.1 臂 action 来源

Harvest OpenSpec 假设两条独立控制后端：

- GELLO：`/left|right/gello/accepted_joint_targets`
- Pico：`/left|right/pico/validated_joint_targets`

本仓库 **GELLO 和 Pico 都经过同一安全网关**。训练用 arm action 只取：

```text
/teleop/validated_arm_commands   sensor_msgs/msg/JointState
```

不要把 `/teleop/arm_commands` 或 `/{side}/gello/joint_states` 当作训练 action。

Pico 仓库的 `teleop_data_collector` 已经按这条 topic 改过：单条消息可能只有 active 一侧，按名字拆分 `left_fr3v2_joint*` / `right_fr3v2_joint*`，缺失侧不填零。本仓库沿用该适配。

`teleoperator` 元数据仍要写 `gello`（第一期）或日后的 `pico`，以便训练时分层；LeRobot 的 54/108 维顺序两边相同。

### 5.2 手

Harvest 手 topic 来自它自己的 ROS Wuji driver：`/wuji/{left,right}/command|joint_states`。

本仓库 Operator/HandWorker 持有唯一 Wuji 连接，ROS 里目前没有这些 topic。必须用 Pico 仓库已实现的只读导出：

```text
现有 HandWorker / WujiHandPipeline
  → 有界非阻塞队列
  → 本机 UDP telemetry sender
  → ROS teleop_hand_telemetry（只 publish）
  → Recorder
```

ROS topic 用本仓库契约名，避免和 Harvest `/wuji/*` 混淆：

```text
/teleop/wuji/{left,right}/command
/teleop/wuji/{left,right}/joint_states
/teleop/wuji/telemetry_status
```

队列满只丢遥测并计数，不能阻塞控制线程。UDP 带 version、session、sequence 和校验。没有实测速度时，用显式有限差分并打标，禁止静默填零冒充实测值。

### 5.3 相机

Harvest：一台 435LE + 两台 305，按序列号绑到 `cam0/cam1/cam2`。

本仓库现有单相机 Orbbec 不能当三相机用。需要独立的 `teleop_camera_bringup`，输出：

```text
/cam{0,1,2}/color/image_raw
```

序列号和物理语义写入 provenance。任何 `REPLACE_*`、空白、重复 SN 都不得启动录制。保留 Orbbec LICENSE/NOTICE 和 SDK runtime 显式许可门禁，不得默认代替用户接受 EULA。

## 6. 数据契约

与 Harvest / Pico 移植保持一致。单位 radian。一律按 joint name 归一化，禁止依赖消息数组下标。

### 6.1 Action `float32[54]`

```text
left arm position      7
right arm position     7
left Wuji command     20
right Wuji command    20
```

关节名：臂用契约中的稳定 `left/right_fr3_joint*`（由 `left/right_fr3v2_joint*` 归一化）；手用写入 `teleop_core.contract` 的 20 个 Wuji 名。

### 6.2 Observation state `float32[108]`

```text
left arm position      7
left arm velocity      7
right arm position     7
right arm velocity     7
left hand position    20
left hand velocity     20
right hand position   20
right hand velocity   20
```

### 6.3 图像

```text
observation.images.cam0 <- /cam0/color/image_raw
observation.images.cam1 <- /cam1/color/image_raw
observation.images.cam2 <- /cam2/color/image_raw
```

### 6.4 与 wuji-openpi 的已知不一致

`/home/user/llx/harvest_ws/wuji-openpi` 当前：

- action 54 维顺序是 **左臂 7、左手 20、右臂 7、右手 20**（不是左臂/右臂再双手）。
- observation.state 默认约 54 维（关节位），不是这里的 108 维（含速度）。
- 相机 feature 名是 `cam_high` / `cam_left_wrist` / `cam_right_wrist` 一类，不是 `cam0/cam1/cam2`。

本任务不修改 OpenPI。文档必须保留该差异；接入训练时用显式 loader/repack，禁止按数组位置静默兼容。

## 7. 建议目录

```text
gello-retarget/
├── 移植采集数据.md                          # 本文件
├── data_collection/
│   ├── README.md
│   ├── contract/dataset_contract.yaml
│   ├── config/record_gello.yaml            # 第一期
│   ├── config/convert_gello_lerobot_v2.yaml
│   ├── config/cameras.yaml                # 部署填 SN，不提交真实密钥也可本地覆盖
│   └── docs/DATA_FORMAT.md
├── ros_ws/src/
│   ├── teleop_data_collector/             # 由 Harvest data_collector 改名
│   ├── teleop_hand_telemetry/
│   ├── teleop_camera_bringup/
│   ├── orbbec_camera/                      # 保留 LICENSE/NOTICE；EULA 门禁
│   └── orbbec_camera_msgs/
├── teleop_runtime/hand_telemetry.py        # 无 ROS 的 UDP codec + 非阻塞 sender
└── ops/run/
    ├── start_recording.sh
    └── convert_recording.sh
```

数据输出必须在仓库外，例如 `/home/user/franka_teleop_data`。

优先从 Pico 仓库拷已适配过的包，再对照 Harvest `data_collector` / OpenSpec 补 GELLO 配置名和 provenance。不要先整树复制 Harvest。

## 8. 分步清单

先固定契约和测试，再选择性拷文件。

### 8.1 契约与边界

- [ ] 在 `teleop_core.contract` 增加 Wuji 左右 20 关节名、command/state/status topic；测试拒绝缺名、重复、乱序。
- [ ] 落地 `data_collection/contract/dataset_contract.yaml`：54/108、三相机、`teleoperator=gello`、源 topic 表；YAML 与 `teleop_core.contract` 互相校验。
- [ ] 增加 `HandTelemetryStatus.msg`，不改现有 ArmCommand 语义。
- [ ] 静态检查：采集包不得 import/启动第二套 Operator、gateway、Wuji/Pico/MANUS SDK client；不得 publish FR3/Wuji 命令。
- [ ] 文档写明 OpenPI 差异。

### 8.2 只读手遥测

- [ ] 拷贝/对齐 `teleop_runtime/hand_telemetry.py`。
- [ ] HandWorker / Wuji pipeline：有界非阻塞 enqueue；满则丢并计数。
- [ ] `start_wuji_teleop.sh` 默认向本机 UDP（建议 5602）导出，采集未启动时不得影响控制。
- [ ] `teleop_hand_telemetry` 只 publish ROS，不收硬件命令。
- [ ] 测试：round-trip、重复、乱序、丢包、stale、崩溃不影响控制线程。

### 8.3 三相机

- [ ] 从 Harvest vendor 拷 `orbbec_camera` / `orbbec_camera_msgs`，保留 LICENSE/NOTICE；普通构建跳过 SDK runtime。
- [ ] 显式构建门禁（如 `--accept-orbbec-eula`）和运行时 `ORBBEC_SDK_LICENSE_ACCEPTED=YES`。
- [ ] `teleop_camera_bringup`：SN → cam0/1/2；拒绝 REPLACE/空白/重复。
- [ ] 与现有 compose `orbbec`、`start_orbbec_viewer.sh` 互斥。
- [ ] 镜像补齐转换依赖（Pico 构建曾卡在容器内 `pyarrow`）。

### 8.4 Recorder 与转换器

对照 Harvest `data_collector` 与 Pico `teleop_data_collector`：

- [ ] `SPACE` 开始/停止；`D` 标 discarded 且不删源；`Ctrl-C` 标 interrupted。
- [ ] 状态仅 `recording/finalized/incomplete/interrupted/discarded`。
- [ ] `collection_state.json` + provenance（teleoperator、workcell hash、相机 SN、源 topic）。
- [ ] 开始前检查 topic 名/类型；停止后检查计数、频率、默认 150 ms gap、header 单调、有限值、双侧 action、双手、三相机。
- [ ] 缺任一臂、手或相机流不得 `finalized`。
- [ ] 只转换 `finalized` bag；临时目录 + 校验后原子发布；转换前后核对源 bag 大小和 mtime。
- [ ] 确定性时间对齐（默认 30 Hz、150 ms），报告 unmatched/dropped。
- [ ] LeRobot v2 目录：`data/`、`videos/.../cam{0,1,2}/`、`meta/info.json` 等，以及独立 `dataloader.py`。

### 8.5 启动入口

遥操（终端 1），现有入口不变：

```bash
ROS_DOMAIN_ID=1 TELEOP_ROS_DOMAIN_ID=1 ./ops/run/start_wuji_teleop.sh \
  --wuji-left-address LEFT_IP:PORT \
  --wuji-right-address RIGHT_IP:PORT
```

采集（终端 2），只能起 receiver + 三相机 + recorder：

```bash
export ORBBEC_SDK_LICENSE_ACCEPTED=YES
./ops/run/start_recording.sh \
  --data-root /home/user/franka_teleop_data
```

手动转换：

```bash
./ops/run/convert_recording.sh \
  /home/user/franka_teleop_data/bags/gello/episode0 \
  /home/user/franka_teleop_data/datasets/episode0
```

第一期两个命令独立。不要把采集焊进 `start_wuji_teleop.sh`。稳定后再考虑可选 `--enable-data-collection`，且内部仍须独立受监管进程。

`start_recording.sh` 必须拒绝：数据目录在仓库内、相机 SN 未部署、EULA 未接受、hand-control/O30i 栈占用、与现有 `orbbec` 服务冲突。

### 8.6 测试与验收

离线测试不得初始化真实 GELLO、Pico、MANUS、Wuji 或 Franka。

- [ ] 原工程测试和 `./ops/run/preflight.sh` 继续通过。
- [ ] action 54 / state 108 精确顺序；`fr3v2` 名称归一化。
- [ ] 三相机 feature 与设备 identity。
- [ ] episode 各状态；合成 ROS bag → LeRobot v2。
- [ ] 转换失败不改源 bag、不暴露半成品。
- [ ] fake Franka 短 episode 录制再手动转换。
- [ ] 杀掉 recorder / telemetry receiver 后控制进程仍运行。

真机（需用户明确授权，单独安排）：

- [ ] 填本地 `cameras.yaml` 三个 SN 和语义。
- [ ] 与遥操同一 `ROS_DOMAIN_ID`。
- [ ] 停掉 Wuji UI、hand-control、单相机 orbbec、OrbbecViewer。
- [ ] 短 episode：SPACE 起停、D discarded、手动 convert。
- [ ] 确认源 bag 仍在、数据集通过 54/108/三相机校验。

## 9. 明确不做

- 不把 Harvest 的 `--teleoperator gello|pico` 整仓 bringup 搬进本仓库。
- 不把 GELLO 改成绕过安全网关去对齐 Harvest 的 `accepted_joint_targets`。
- 不把 `teleop_data`（O30i）和 Harvest collector 合成一个包。
- 第一期不实现 Pico 臂录制配置；若以后做，只加 `record_pico.yaml`，LeRobot 特征不变，metadata 写 `teleoperator=pico`。
- 不 commit 现场 IP、相机 SN、密钥。

## 10. 完成时应交付

- 新增/修改文件清单。
- 架构图：控制进程 vs 采集进程，以及谁能 publish 什么。
- topic / 消息类型表。
- action/state 精确顺序。
- 构建、遥操、录制、转换命令。
- 离线测试结果；未完成的真机项单独列出。
- license/NOTICE 与 Orbbec EULA 说明。
- Harvest / OpenPI 格式差异。

## 11. 建议实施顺序

1. 契约 YAML + `teleop_core.contract` + 失败测试。
2. 手 UDP 遥测（不碰相机）。
3. 从 Pico 仓库拷 `teleop_data_collector`，改成 `record_gello.yaml` / `bags/gello`。
4. 三相机 bringup + EULA 门禁。
5. `start_recording.sh` / `convert_recording.sh`。
6. 合成 bag 与 fake Franka 验收。
7. 真机短 episode（单独授权）。
