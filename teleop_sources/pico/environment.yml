name: franka-teleop-pico
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pybind11
  - pyyaml
  # Hand retargeting: scipy provides L-BFGS-B. Forward kinematics and frame
  # Jacobians come from pinocchio, which pin-pink in pyproject.toml already
  # pulls in for the arm IK.
  - scipy
