# Harvest 数据采集：进度

对照 [移植采集数据.md](移植采集数据.md)。日期 2026-08-31。第一期目标仍是 **GELLO 臂 + Wuji 手 + 三相机**，采集只读、独立进程，不破坏现有 `start_wuji_teleop.sh`。

当前状态：**代码、离线契约测试、本机镜像增量层（pyarrow/opencv）和 Orbbec colcon 已落地。preflight、容器换新层后的遥操拉起、合成 bag 转换、fake Franka、真机短 episode 未做。**

---

## 已完成

### 契约与边界

- `teleop_core.contract` 增加稳定臂名、Wuji 左右各 20 关节名、command/state/status topic；`require_exact_joint_names` 拒绝缺名、重复、乱序。
- `data_collection/contract/dataset_contract.yaml`：54/108、三相机、`teleoperator=gello`、源 topic 表；测试与 `teleop_core.contract` 互校。
- 新增 `HandTelemetryStatus.msg`，未改 `ArmCommand`。
- 静态检查：采集包不得 import 第二套 Operator / gateway / Wuji·Pico·MANUS SDK client，不得 publish FR3 命令总线。
- OpenPI 差异写在 `data_collection/docs/DATA_FORMAT.md`。

### 只读手遥测

- `teleop_runtime/hand_telemetry.py`：UDP codec、校验、有界非阻塞队列（满则丢并计数）。
- `HandWorker` 与 `WujiHandPipeline`：成功 send 后才缓存 command；telemetry 失败不进控制路径。
- `start_wuji_teleop.sh` 未改；`--hand-source wuji` 时 Operator 默认发往 `127.0.0.1:5602`。可用 `--disable-hand-telemetry`。
- `teleop_hand_telemetry` 只 publish `/teleop/wuji/...`，不收硬件命令。
- 测试覆盖 round-trip、重复、乱序、丢包、stale、队列满不阻塞、sender 崩溃不停 HandWorker。

### 三相机与 Orbbec（代码侧）

- 从 Pico（其源来自 Harvest vendor）拷入 `orbbec_camera` / `orbbec_camera_msgs`，保留 LICENSE/NOTICE/EULA。
- 默认 `COLCON_IGNORE`，避免 overlay 现有 compose `orbbec`（`/camera/*`）。
- `./ops/setup/build.sh --accept-orbbec-eula` 编 SDK；运行时读本机 `docker/.env` 里的 `ORBBEC_SDK_LICENSE_ACCEPTED=YES`，不必每次 `export`。
- `teleop_camera_bringup`：SN → cam0/1/2，拒绝 REPLACE / 空白 / 重复。
- `start_recording.sh` 拒绝 hand-control、compose `orbbec`、OrbbecViewer。
- Dockerfile 末层已加系统 Python `pyarrow==17.0.0` 和 `python3-opencv`。本机未从 ROS 基础层重编：在已有 `latest` 上加了一层（2026-08-31，`./ops/setup/build.sh --accept-orbbec-eula` 退出码 0）。`COLCON_IGNORE` 已加回。

### Recorder / 转换器 / 入口（代码侧）

- `teleop_data_collector`：SPACE / D / Ctrl-C；五态；provenance；finalized 才转换；原子发布；30 Hz / 150 ms 对齐。
- 配置：`record_gello.yaml`、`convert_gello_lerobot_v2.yaml`，输出 `bags/gello`。
- 入口独立，未焊进 `start_wuji_teleop.sh`：

```bash
# 终端 1
ROS_DOMAIN_ID=1 TELEOP_ROS_DOMAIN_ID=1 ./ops/run/start_wuji_teleop.sh \
  --wuji-left-address LEFT_IP:PORT \
  --wuji-right-address RIGHT_IP:PORT

# 终端 2（本机 docker/.env 已写 ORBBEC_SDK_LICENSE_ACCEPTED=YES）
./ops/run/start_recording.sh --data-root /home/user/franka_teleop_data

# 手动转换
./ops/run/convert_recording.sh \
  /home/user/franka_teleop_data/bags/gello/episode0 \
  /home/user/franka_teleop_data/datasets/episode0
```

`start_recording.sh` 已拒绝：数据目录在仓库内、相机 SN 未部署、EULA 未接受、hand-control、compose `orbbec`。

### 文档与离线测试

- `data_collection/README.md`：架构、谁能 publish、命令。
- `data_collection/docs/DATA_FORMAT.md`：topic 表、54/108 顺序、OpenPI 差异。
- `data_collection/docs/ORBBEC_PROVENANCE.md`：LICENSE 哈希与本仓库 COLCON_IGNORE 说明。
- 宿主离线 pytest：**63 passed**（契约、遥测、采集边界、Wuji pipeline、HandWorker）。
- 2026-08-31 `build.sh --accept-orbbec-eula` 在容器内 `colcon` 过 15 个包，含 `orbbec_camera` / `orbbec_camera_msgs` / `teleop_camera_bringup` / `teleop_data_collector` / `teleop_hand_telemetry`。stderr 仅为未用的 `LIA_ENABLE_LICENSE_GATED_ORBBEC_RUNTIME` 和容器内无 pytest，不是失败。

未改：`start_wuji_teleop.sh`、`start_teleop.sh`、`teleop_data`、safety gateway、ArmCommand 语义。

## 重建镜像会不会改掉现有遥操 Docker？

会换**同一张**镜像 `franka-upper-body-teleop:latest`。compose 里 `franka-control`、`teleop-control`、`gello-bridge` 等用的都是它，没有第二套镜像。

- 不是再起一套并行 Docker；是给现有 tag 加一层（系统 Python 的 pyarrow/opencv，以及可选的 Orbbec SDK 构建）。
- 代码仍是宿主机 bind mount，遥操控制链没有改 topic 或安全门。
- 做法：先停当前栈 → `./ops/setup/build.sh`（需要三相机时再加 `--accept-orbbec-eula`）→ 再用 `start_wuji_teleop.sh` 拉起。正在跑的容器不会自动换成新镜像，必须重启。

本机镜像已有 pyarrow；`convert_recording.sh` 仍须在换到新层的容器里跑。未换容器则仍是旧层。

## 停掉 hand-control / orbbec 之后怎么遥操？

那些不是 Wuji 遥操用的服务。第一期采集跟的是 **GELLO + MANUS + Wuji**，入口仍是 `start_wuji_teleop.sh`。

| 服务 | `start_teleop.sh`（O30i） | `start_wuji_teleop.sh` | `start_recording.sh` |
| --- | --- | --- | --- |
| `franka-control` `teleop-control` `gello-bridge` | 要 | **要，保持运行** | 不启动，只订阅 |
| `hand-control`（O30i/G20） | 要 | 禁止（脚本已互斥） | 拒绝 |
| compose `orbbec`（单路 `/camera/*`） | 可选 | 不启动 | 拒绝（和三相机抢设备） |
| OrbbecViewer | 调相机时 | 不需要 | 拒绝 |
| 宿主机 Operator + Wuji | 不用 Wuji | **要，保持运行** | 不启动 |

所以：终端 1 继续 `start_wuji_teleop.sh`；终端 2 只起采集。若当前跑的是 `start_teleop.sh`（带 hand-control），那是 O30i 模式，不能边采 Harvest 边用，需要停掉后改走 Wuji 入口。

---

## 需要完成

按阻塞顺序。未获明确授权前不启动真机运动。

### 1. 构建（采集能跑起来的前提）

同一镜像名 `franka-upper-body-teleop:latest`，遥操和采集都用它。先停栈、重建、再按 Wuji 入口拉起。不是另做一套 Docker。

- [x] 本机 `latest` 已带系统 Python `pyarrow` / `python3-opencv`（增量层，不是从 Dockerfile 第 2 层重编）。完整从零重编用 `--rebuild-image`；本机 Clash `127.0.0.1:7897` 时 `build.sh` 会加 `--network=host`（容器里的 127.0.0.1 打不到宿主机代理）。
- [x] `./ops/setup/build.sh --accept-orbbec-eula`：Orbbec overlay 已编进 `ros_ws/install`，随后恢复 `COLCON_IGNORE`
- [ ] 确认 `./ops/run/preflight.sh` 仍通过（不削弱 freshness / acquisition / limit / slew）
- [ ] 停栈后用 `start_wuji_teleop.sh` 拉起，使正在跑的容器换到新镜像层（正在跑的容器不会自动换）

### 2. 离线 ROS 验收（不连真机）

- [ ] 容器内跑 `test_conversion_integration.py`：合成 bag → LeRobot v2，核对 54/108、三相机、源 bag 大小/mtime 不变、失败不留半成品
- [ ] fake Franka 短 episode：`start_wuji_teleop.sh --fake-franka` + `start_recording.sh`，SPACE 起停后手动 `convert_recording.sh`
- [ ] 杀掉 recorder / telemetry receiver 后，Operator 与 FR3 控制进程仍运行

### 3. 真机短 episode（需单独授权）

- [x] 本地填写 `data_collection/config/cameras.yaml` 三个唯一 SN 和物理语义（`cameras.yaml` 已 gitignore，不要 commit）
- [ ] 与遥操同一 `ROS_DOMAIN_ID` / `TELEOP_ROS_DOMAIN_ID`
- [ ] 停掉会抢手/相机的东西：Wuji UI、`hand-control`（O30i）、compose `orbbec`（单相机）、OrbbecViewer。不要停 `start_wuji_teleop.sh` 那一套（franka-control / teleop-control / gello-bridge / Operator）
- [ ] `docker/.env` 已有 `ORBBEC_SDK_LICENSE_ACCEPTED=YES` 后短录：SPACE 起停、D discarded、手动 convert
- [ ] 确认源 bag 仍在，数据集通过 54/108/三相机校验

### 4. 明确不做（仍保持）

- 不把 Harvest 整仓 bringup 或 Pico 臂 `record_pico.yaml` 做进第一期
- 不绕过安全网关去对齐 Harvest 的 `accepted_joint_targets`
- 不把 `teleop_data` 和 Harvest collector 合成一个包
- 不自动转换、上传或删除源 bag
- 不 commit 现场 IP、相机 SN、密钥

---

## 架构与契约（已落地，验收时对照）

```text
终端 1  Operator（唯一 GELLO/MANUS/Wuji client）
  └─ 有界非阻塞队列 → UDP 127.0.0.1:5602
                         └─ teleop_hand_telemetry（仅 ROS publish）

safety gateway → /teleop/validated_arm_commands ─┐
FR3 实测 ─────────────────────────────────────────┤
/cam{0,1,2}/color/image_raw ──────────────────────┼→ ros2 bag record
/teleop/wuji/{left,right}/command|joint_states ────┘
```

| 进程 | 可以 publish | 禁止 |
| --- | --- | --- |
| safety gateway | `/teleop/validated_arm_commands`、FR3 command bus | 第二套控制源 |
| Operator / HandWorker | Wuji SDK 命令；本机 UDP telemetry | ROS 手/臂命令 |
| `teleop_hand_telemetry` | `/teleop/wuji/...`、status | 任何硬件命令 |
| `teleop_camera_bringup` | `/cam{0,1,2}/color/image_raw` | FR3/Wuji 命令 |
| `teleop_data_collector` | 无（只 `ros2 bag record`） | 任何 robot/hand 命令 |

训练 action 只取 `/teleop/validated_arm_commands`。顺序：左臂 7、右臂 7、左手 20、右手 20。observation.state 108 维含速度。单位 radian，按 joint name 归一化。

与 wuji-openpi 的差异（本任务不改 OpenPI）：action 交错顺序不同；state 54 vs 108；相机名为 `cam_high` 等而非 `cam0/1/2`。接入必须显式 repack。

---

## 主要新增 / 修改文件

新增：`data_collection/`，`teleop_runtime/hand_telemetry.py`，`ops/run/start_recording.sh`、`convert_recording.sh`、`run_data_collection_container.sh`，`ros_ws/src/{teleop_data_collector,teleop_hand_telemetry,teleop_camera_bringup,orbbec_camera,orbbec_camera_msgs}`，`HandTelemetryStatus.msg`。

修改：`teleop_core/contract.py`，`HandWorker`，`WujiHandPipeline`，`teleop_runtime/cli.py`，`ops/setup/build.sh`，`docker/Dockerfile`，`README.md`，`THIRD_PARTY_NOTICES.md`。
